{
  "name": "blog",
  "version": "1.0.0",
  "description": "Blog with Markdown to HTML conversion",
  "scripts": {
    "convert": "node scripts/convert_md_to_html.js",
    "watch": "nodemon --watch articles/md --ext md --exec npm run convert",
    "copy-assets": "node scripts/copy-assets.js",
    "convert-blocks": "node scripts/convert-custom-blocks.js",
    "cleanup": "node scripts/cleanup.js",
    "build": "npm run copy-assets && npm run convert-blocks && npm run convert",
    "build:clean": "npm run cleanup && npm run build"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^9.1.0",
    "jsdom": "^22.1.0",
    "highlight.js": "^11.9.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
} 